{
  "name": "dot-files",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "dev": "turbo run dev --filter=macbook",
    "format": "turbo run format",
    "check": "turbo run build test"
  },
  "devDependencies": {
    "turbo": "2.9.8"
  }
}
